new class AdminDashboard {
    constructor() {
        this.bindEvents();
    }

    bindEvents() {
        $(document).ready(() => {
            this.initDashboard();
        });
    }

    initDashboard(){
        let charts = document.getElementById("myChart");

    }
}